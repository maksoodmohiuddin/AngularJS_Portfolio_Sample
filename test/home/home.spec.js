'use strict';

describe('home tests specs', function () {

    beforeEach(module('ambassadors'));

    describe('homeController controller', function () {

        it('should be defined', inject(function ($controller) {
            //spec body
           
        }));

    });
});